# 应用层

`application/` 只放最高层业务逻辑：应用设置、语义输入到页面动作的状态机、串口命令和 FreeRTOS 任务编排。

这里可以依赖 `middleware/` 提供的显示、输入、资源、公共类型和存储接口，但不直接包含 `msp430.h`、`platform_config.h`、GPIO 寄存器、触摸通道、Flash 地址或面板时序。

适合放在这里的代码：

- 面向产品语义的状态机和参数调整逻辑。
- RTOS 任务之间的编排和通知关系。
- 不关心硬件来源的应用策略配置。

封装的目标是降低耦合、让调用关系清楚，不是隐藏启动顺序或把所有流程包成一个大入口。
