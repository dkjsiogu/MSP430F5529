# 应用层

应用层放业务语义、运行状态和顶层调度入口。`main.cpp` 保持纯入口，只调用
`application::start()`；初始化顺序、FreeRTOS 任务创建和轮询调度放在应用层实现里，
避免把硬件寄存器调用散落到入口文件。

这里适合放：

- 页面、采样、报警等业务状态类型
- 面向应用语义的轻量工具和任务入口
- 不直接依赖 FatFs/Flash/寄存器细节的接口

封装的目标是降低模块耦合、让入口清晰，不是把执行顺序藏进难调试的大对象。
